{
  "name": "@steam-auction/desktop",
  "version": "26.0",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "tauri": "tauri",
    "build": "command -v cargo >/dev/null 2>&1 && tauri build || echo 'Skipping desktop build: Cargo/Rust not installed. Install Rust from https://rustup.rs/'"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}